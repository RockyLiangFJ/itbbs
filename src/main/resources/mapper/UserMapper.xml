<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.missionsky.bbs.dao.mapper.UserMapper">








    <!-- 通过二级板块获取帖子 -->
    <select id="getUser" resultType="com.missionsky.bbs.dto.LoginRequest" parameterType="com.missionsky.bbs.dto.LoginRequest">
   	 SELECT * FROM user WHERE user_name = #{userName} AND password = #{password}
</select>








</mapper>